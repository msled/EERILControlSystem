<Window x:Class="EERIL.ControlSystem.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestWindow" Height="300" Width="300" Closed="Window_Closed" ResizeMode="NoResize">
    <DockPanel LastChildFill="True" Margin="12">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 12px 0 0">
            <Button Name="beginButton" Content="Begin" Margin="0 0 12 0" IsDefault="True" IsEnabled="False" />
            <Button Name="cancelButton" Content="Cancel" IsCancel="True" Click="cancelButton_Click" />
        </StackPanel>
        <TreeView Name="testTreeView" SelectedItemChanged="testTreeView_SelectedItemChanged" />
    </DockPanel>
</Window>
